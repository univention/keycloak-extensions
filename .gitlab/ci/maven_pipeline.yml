---
workflow:
  rules:
    - if: '$FILENAME_RECAPTCHA_PLUGIN_JAR == null'
      variables:
        FILENAME_RECAPTCHA_PLUGIN_JAR: "reCaptcha-auth-flow.jar"

.maven_job_template: &maven_job
  image: "artifacts.knut.univention.de/dockerhub_proxy_cache/library/maven:3.8.6-openjdk-18"
  cache:
    - key:
        files:
          - "reCaptcha-auth-flow/pom.xml"
      paths:
        - ".m2/repository"
  variables:
    MAVEN_OPTS: "-Dhttps.protocols=TLSv1.2 -Dmaven.repo.local=$CI_PROJECT_DIR/.m2/repository -Dorg.slf4j.simpleLogger.log.org.apache.maven.cli.transfer.Slf4jMavenTransferListener=WARN -Dorg.slf4j.simpleLogger.showDateTime=true -Djava.awt.headless=true"
    MAVEN_CLI_OPTS: "--batch-mode --errors --fail-at-end --show-version -DinstallAtEnd=true -DdeployAtEnd=true"

retrieve_maven_version:
  <<: *maven_job
  stage: build
  script:
    - 'version="$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)"'
    - 'echo "MAVEN_PROJECT_VERSION=$version" >> maven.env'
  artifacts:
    reports:
      dotenv: "maven.env"

# Inspired by: https://gitlab.com/gitlab-org/gitlab-ci-yml/-/blob/master/Maven.gitlab-ci.yml
maven_clean_install:
  <<: *maven_job
  stage: build
  needs:
    - "retrieve_maven_version"
  script:
    - cd reCaptcha-auth-flow
    - mvn clean install
    - mv target/$FILENAME_RECAPTCHA_PLUGIN_JAR ..
  artifacts:
    paths:
      - "./$FILENAME_RECAPTCHA_PLUGIN_JAR"