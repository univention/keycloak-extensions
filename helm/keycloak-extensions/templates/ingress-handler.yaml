# SPDX-License-Identifier: AGPL-3.0-only
# SPDX-FileCopyrightText: 2023 Univention GmbH

{{- if .Values.handler.ingress.enabled -}}
---

apiVersion: {{ include "common.capabilities.ingress.apiVersion" . }}
kind: Ingress
metadata:
  name: {{ printf "%s-proxy" (include "common.names.fullname" .) }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
  {{- with .Values.handler.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.handler.ingress.ingressClassName }}
  ingressClassName: {{ .Values.handler.ingress.ingressClassName }}
  {{- end }}
  {{- if .Values.handler.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.handler.ingress.host | quote }}
      secretName: "{{ .Values.handler.ingress.tls.secretName }}"
  {{- end }}
  rules:
    - host: {{ .Values.handler.ingress.host | quote }}
      http:
        paths:
          {{- range .Values.handler.ingress.paths }}
          - pathType: {{ .pathType }}
            path: {{ .path }}
            {{- if .backend }}
            backend: {{- (toYaml .backend) | nindent 14 }}
            {{- else }}
            backend: {{- default (include "common.ingress.backend" (dict "serviceName" (include "common.names.fullname" $) "servicePort" "http" "context" $)) | nindent 14 }}
            {{- end }}
          {{- end }}

...
{{- end }}
