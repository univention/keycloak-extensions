{{/*
SPDX-FileCopyrightText: 2024 Univention GmbH
SPDX-License-Identifier: AGPL-3.0-only
*/}}

# NOTE: We may need a utility function, so that it is easy to handle corner
# cases correctly.
{{- if (not (coalesce .Values.smtp.auth.existingSecret.name .Values.global.smtp.auth.existingSecret.name )) }}

---
kind: "Secret"
apiVersion: "v1"
type: "kubernetes.io/basic-auth"
metadata:
  name: {{ include "common.names.fullname" . }}-smtp
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "common.labels.standard" . | nindent 4 }}
    {{- if .Values.additionalLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.additionalLabels "context" . ) | nindent 4 }}
    {{- end }}
    dev: mark
  {{- if .Values.additionalAnnotations }}
  annotations:
    {{- include "common.tplvalues.render" ( dict "value" .Values.additionalAnnotations "context" . ) | nindent 4 }}
  {{- end }}
stringData:
  username: {{ coalesce .Values.smtp.auth.username .Values.global.smtp.auth.username | quote }}
  password: {{ coalesce .Values.smtp.auth.password .Values.global.smtp.auth.password | quote }}

{{- end }}
